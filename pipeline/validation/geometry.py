"""Section B — boundaries and geometry.

Checks that the shapefiles behind every geography level are the ones the
registry says they are, and that the simplified geometry the map serves is a
faithful reduction of them.

Not yet implemented. Registering nothing is deliberate: an empty section in the
report is honest, where a section of checks that trivially pass would be worse
than no section at all.
"""

from __future__ import annotations
